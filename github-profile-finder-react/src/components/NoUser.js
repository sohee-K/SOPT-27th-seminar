import React from 'react';

function NoUser() {
    return (
        <div style={{ color: "white", fontSize: "2rem" }}>
            There's no such user.
        </div>
    );
}

export default NoUser;